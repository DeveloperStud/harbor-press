# Server-only. Never expose this to the browser.
# Get a key: https://opensea.io/settings/developer
# Instant free-tier (rate limited): POST https://api.opensea.io/api/v2/auth/keys
OPENSEA_API_KEY=

# Public site URL used in metadata packages
NEXT_PUBLIC_SITE_URL=http://localhost:3000
